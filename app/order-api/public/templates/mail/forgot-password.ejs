<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Request Forgot Password</title>
    <% var dir=process.cwd() %>
    <style>
      <%- include(dir + '/public/css/tailwind.min.css') %>
    </style>
  </head>
  <body class="bg-gray-100">
    <div class="max-w-md mx-auto bg-white shadow-lg rounded-lg overflow-hidden">
      <div class="px-6 py-4">
        <h1 class="text-xl font-semibold text-gray-800">Reset Your Password</h1>
        <p class="text-gray-600 mt-2">Hi <%= locals.name %>,</p>
        <p class="text-gray-600 mt-2">
          We received a request to reset your password. The code is:
        </p>
        <div class="mt-4 mb-4 p-4 bg-gray-100 text-center rounded">
          <p class="text-2xl font-bold tracking-widest text-blue-600">
            <%= locals.code %>
          </p>
        </div>
        <p class="text-gray-600 mt-4">
          This code will expire at
          <span class="font-bold"><%= locals.expiresAt %></span>.
        </p>
        <p class="text-gray-600 mt-4">
          If you didn’t request reset your password, please ignore this email.
        </p>
      </div>
    </div>
  </body>
</html>

